<Header>
  <nav class="navbar navbar-expand-lg bg-secondary">
    <div class="container">
      <a class="navbar-brand fs-3 fw-bold" href="#">Codieal</a>
      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/">Home</a>
          </li>

          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/user/profile"
              >Profile</a
            >
          </li>
        </ul>
        <% if(locals.user) { %>
        <div class="d-flex align-items-baseline">
          <p class="me-2 text-light fw-bold"><%= locals.user.name %></p>
          <a class="btn btn-danger" href="/user/signout"> signout </a>
        </div>

        <%} else{ %>
        <a class="btn btn-primary" href="/user/login"> Login </a>
        <%}%>
      </div>
    </div>
  </nav>
</Header>
